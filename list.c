//reverse a list
//Reverse a linked list with constant space.
How to detect a loop in a linked list
have two pointers moving faster and one pointer moving one after other. 
If the slow pointer meets any of the faster pointer, then the list is circular. If the list is not circular, 
either the fast or slow pointer will hit a NULL and we can use that as a condition to exit the function
